<!DOCTYPE html>
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet", href="/css/normalize.css")
    link(rel="stylesheet", href="/css/app.css")
    script(src="https://kit.fontawesome.com/2322ff0561.js")
    title= tituloPag
  body
    header.barra
      h1 Administrador de Proyectos
      a(href="#") Cerrar sesión

    main.contenedor
      aside.contenedor-proyectos
        .panel.crear-proyecto
          a(href="/nuevo-proyecto" class="boton") Nuevo Proyecto

        .panel.lita-proyectos 
          h2 Proyectos
          ul#proyectos.proyectos
            if(proyectos.length)
              each proyecto in proyectos
                li
                  a(href=`/proyecto/${proyecto.url}`)= proyecto.nombre

      block contenido

      //- pre= vardump(proyectos)
    script(src="/dist/bundle.js")